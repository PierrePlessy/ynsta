%nav{class: 'navbar navbar-default'}
  .container-fluid
    .navbar-header
      %a{class: 'navbar-brand', href: root_path}
        = 'Ynsta'

    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.active
          %a{href: '#'}
            = 'People'
        %li
          %a{href: '#'}
            = 'Travel'
        %li
          %a{href: '#'}
            = 'Food'

      .form{class:'navbar-form navbar-left'}
        .form-group
          %input{input: 'text', class:'form-control', placeholder:'Rechercher'}

      - if current_user
        %a{href: '#', class: ' pull-right'}
          = 'Uploader une image'
      - else
        %a{href: new_user_session_path, class: ' pull-right'}
          = 'Inscription'
        %a{href: new_user_session_path, class: ' pull-right'}
          = 'Connexion'
